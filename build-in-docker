#!/bin/bash
set -euxo pipefail
dist="$1"

cp -r /mnt /work
cd /work

export DEBIAN_FRONTEND=noninteractive
apt-get update
apt-get -y install --no-install-recommends build-essential devscripts equivs
mk-build-deps -i --tool 'apt-get --no-install-recommends -y'

VISUAL=touch dch --local "~$dist"
debuild -us -uc -sa

shopt -s nullglob
install -o "$DIST_UID" -g "$DIST_GID" ../{*.changes,*.deb,*.dsc,*.tar.*,*.buildinfo} /mnt/dist/
